---
phase: 01-scaffolding
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - .github/workflows/keep-warm.yml
  - .gitignore
  - frontend/.gitignore
  - backend/.gitignore
autonomous: false
requirements: []
user_setup:
  - service: supabase
    why: "Database, auth, and realtime — core data layer"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase Dashboard -> Settings -> API -> Project URL"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase Dashboard -> Settings -> API -> anon/public key (or 'publishable key' on newer projects)"
      - name: SUPABASE_URL
        source: "Same as NEXT_PUBLIC_SUPABASE_URL (for FastAPI)"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard -> Settings -> API -> service_role key (or 'secret key' on newer projects)"
    dashboard_config:
      - task: "Create a new Supabase project"
        location: "https://supabase.com/dashboard -> New Project"
  - service: railway
    why: "FastAPI backend hosting"
    env_vars:
      - name: RAILWAY_BACKEND_URL
        source: "Railway Dashboard -> Service -> Settings -> Domains (public URL)"
    dashboard_config:
      - task: "Create Railway project, add service from GitHub repo with root directory /backend"
        location: "https://railway.app/dashboard"
      - task: "Set env vars: SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, ALLOWED_ORIGINS"
        location: "Railway Dashboard -> Service -> Variables"
  - service: vercel
    why: "Next.js frontend hosting"
    dashboard_config:
      - task: "Import GitHub repo, set root directory to /frontend"
        location: "https://vercel.com/new"
      - task: "Set env vars: NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, NEXT_PUBLIC_API_URL (Railway URL)"
        location: "Vercel Dashboard -> Project -> Settings -> Environment Variables"
  - service: github
    why: "Keep-warm workflow needs backend URL secret"
    env_vars:
      - name: RAILWAY_BACKEND_URL
        source: "The deployed Railway public URL (e.g. https://your-service.up.railway.app)"
    dashboard_config:
      - task: "Add RAILWAY_BACKEND_URL as a repository secret"
        location: "GitHub repo -> Settings -> Secrets and variables -> Actions -> New repository secret"

must_haves:
  truths:
    - "GitHub Actions keep-warm workflow pings /health every 4 minutes"
    - ".env and .env.local files are gitignored"
    - "Both frontend and backend have proper .gitignore files"
    - "Cross-service communication works: frontend can fetch from FastAPI without CORS errors"
  artifacts:
    - path: ".github/workflows/keep-warm.yml"
      provides: "Scheduled ping to prevent Railway cold starts"
      contains: "*/4 * * * *"
    - path: ".gitignore"
      provides: "Root gitignore covering both subprojects"
      contains: ".env"
    - path: "backend/.gitignore"
      provides: "Backend-specific gitignore"
      contains: "__pycache__"
    - path: "frontend/.gitignore"
      provides: "Frontend-specific gitignore"
      contains: ".env.local"
  key_links:
    - from: ".github/workflows/keep-warm.yml"
      to: "RAILWAY_BACKEND_URL secret"
      via: "GitHub Actions secret reference"
      pattern: "secrets\\.RAILWAY_BACKEND_URL"
---

<objective>
Wire the integration layer: gitignore configuration, GitHub Actions keep-warm workflow, and verify cross-service communication between frontend and backend.

Purpose: Ensure the full three-tier stack is connected and operational. The keep-warm workflow prevents Railway cold starts during live tournaments. The checkpoint verifies the deployment pipeline works end-to-end.
Output: Complete scaffolding with all services communicating and keep-warm active.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scaffolding/01-CONTEXT.md
@.planning/phases/01-scaffolding/01-RESEARCH.md
@.planning/phases/01-scaffolding/01-VALIDATION.md
@.planning/phases/01-scaffolding/01-01-SUMMARY.md
@.planning/phases/01-scaffolding/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create gitignore files and GitHub Actions keep-warm workflow</name>
  <files>.gitignore, frontend/.gitignore, backend/.gitignore, .github/workflows/keep-warm.yml</files>
  <action>
1. `.gitignore` (root) — Add entries for:
   - `node_modules/`
   - `.env`
   - `.env.local`
   - `.env*.local`
   - `__pycache__/`
   - `*.pyc`
   - `.pytest_cache/`
   - `dist/`
   - `.next/`
   - `.vercel/`

2. `backend/.gitignore` — Add:
   - `__pycache__/`
   - `*.pyc`
   - `.env`
   - `.pytest_cache/`
   - `*.egg-info/`
   - `.venv/`
   - `venv/`

3. `frontend/.gitignore` — Add:
   - `node_modules/`
   - `.next/`
   - `.env.local`
   - `.env*.local`
   - `.vercel`

4. `.github/workflows/keep-warm.yml` — GitHub Actions workflow:
   - Name: "Keep Railway Warm"
   - Triggers: `schedule` with cron `*/4 * * * *` (every 4 minutes) and `workflow_dispatch` (manual trigger)
   - Single job `ping` on `ubuntu-latest`
   - Single step: `curl -f ${{ secrets.RAILWAY_BACKEND_URL }}/health || exit 1`
   - Add timeout-minutes: 1 to the job
  </action>
  <verify>
    <automated>cat .github/workflows/keep-warm.yml | grep "*/4" && echo "Cron schedule found" || echo "Missing cron schedule"</automated>
  </verify>
  <done>.gitignore files exist for root, frontend, and backend. keep-warm.yml has 4-minute cron schedule referencing RAILWAY_BACKEND_URL secret.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete scaffolding works end-to-end</name>
  <action>Human verifies that the full three-tier scaffolding is functional: backend tests pass, frontend builds, and cross-service communication works locally.</action>
  <what-built>Complete three-tier scaffolding: FastAPI backend (with tests), Next.js frontend (with Supabase SSR clients), gitignore files, and GitHub Actions keep-warm workflow. All code is ready for deployment.</what-built>
  <how-to-verify>
    1. Backend verification:
       - Run `cd backend && pip install -r requirements.txt && pytest tests/ -v`
       - All tests should pass (health endpoint + config loading)

    2. Frontend verification:
       - Run `cd frontend && npm install && npm run build`
       - Build should complete without errors

    3. Local dev verification:
       - Terminal 1: `cd backend && cp .env.example .env` (fill in Supabase credentials), then `uvicorn main:app --reload`
       - Terminal 2: `cd frontend && cp .env.example .env.local` (fill in credentials), then `npm run dev`
       - Visit http://localhost:3000 — should see "PicklePoint" heading
       - Visit http://localhost:8000/health — should see {"status": "ok"}
       - Open browser console on localhost:3000, run: `fetch('http://localhost:8000/health').then(r => r.json()).then(console.log)` — should return {"status": "ok"} without CORS errors

    4. File structure check: Confirm no .env files are tracked by git (`git status` should not show .env or .env.local)

    NOTE: Deployment to Vercel/Railway/Supabase is a user setup task. Complete the user_setup items in this plan's frontmatter before deploying. Production cross-service verification happens after deployment.
  </how-to-verify>
  <verify>Human confirms local dev environment works</verify>
  <done>User has verified: backend tests pass, frontend builds, cross-origin fetch works locally without CORS errors.</done>
  <resume-signal>Type "approved" if local dev works, or describe issues encountered</resume-signal>
</task>

</tasks>

<verification>
- backend: `cd backend && pytest tests/ -v` — all green
- frontend: `cd frontend && npm run build` — exits 0
- gitignore: `git status` does not show .env files
- keep-warm: `.github/workflows/keep-warm.yml` exists with */4 cron
</verification>

<success_criteria>
- Both backend tests pass
- Frontend builds without errors
- Local dev: both servers start, CORS works between them
- .env files are properly gitignored
- GitHub Actions keep-warm workflow is ready (activates after RAILWAY_BACKEND_URL secret is set)
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffolding/01-03-SUMMARY.md`
</output>
