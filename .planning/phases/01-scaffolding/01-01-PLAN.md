---
phase: 01-scaffolding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/main.py
  - backend/config.py
  - backend/routers/__init__.py
  - backend/routers/health.py
  - backend/requirements.txt
  - backend/railway.toml
  - backend/.env.example
  - backend/pytest.ini
  - backend/tests/__init__.py
  - backend/tests/test_health.py
  - backend/tests/test_config.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "FastAPI app starts with uvicorn and responds at /health with 200"
    - "Settings class validates SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY from environment"
    - "CORS middleware is configured with origins from ALLOWED_ORIGINS env var"
    - "Tests pass confirming health endpoint and config loading"
  artifacts:
    - path: "backend/main.py"
      provides: "FastAPI app with CORS middleware and router mounts"
      contains: "CORSMiddleware"
    - path: "backend/config.py"
      provides: "pydantic-settings Settings class"
      contains: "class Settings"
    - path: "backend/routers/health.py"
      provides: "GET /health endpoint"
      exports: ["router"]
    - path: "backend/railway.toml"
      provides: "Railway deployment configuration"
      contains: "startCommand"
    - path: "backend/tests/test_health.py"
      provides: "Health endpoint test"
      contains: "test_health"
    - path: "backend/tests/test_config.py"
      provides: "Config loading test"
      contains: "test_settings"
  key_links:
    - from: "backend/main.py"
      to: "backend/config.py"
      via: "get_settings() import"
      pattern: "from config import get_settings"
    - from: "backend/main.py"
      to: "backend/routers/health.py"
      via: "router include"
      pattern: "app\\.include_router"
---

<objective>
Scaffold the FastAPI backend with health endpoint, CORS configuration, pydantic-settings, Railway deployment config, and pytest test suite.

Purpose: Establish the backend service that will serve as the API layer between the frontend and Supabase. This is the first half of the three-tier stack.
Output: A working FastAPI app in `/backend` with passing tests and Railway deployment config.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scaffolding/01-CONTEXT.md
@.planning/phases/01-scaffolding/01-RESEARCH.md
@.planning/phases/01-scaffolding/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FastAPI backend with health endpoint and CORS</name>
  <files>backend/main.py, backend/config.py, backend/routers/__init__.py, backend/routers/health.py, backend/requirements.txt, backend/railway.toml, backend/.env.example</files>
  <action>
Create the `/backend` directory and all files:

1. `backend/config.py` — pydantic-settings `Settings` class with fields: `supabase_url: str`, `supabase_service_role_key: str`, `allowed_origins: str = "http://localhost:3000"`, `port: int = 8000`. Add `origins_list` property that splits `allowed_origins` on comma. Use `SettingsConfigDict(env_file=".env", env_file_encoding="utf-8")`. Add `@lru_cache` `get_settings()` factory function.

2. `backend/routers/__init__.py` — empty file.

3. `backend/routers/health.py` — `APIRouter` with `GET /health` returning `{"status": "ok"}`.

4. `backend/main.py` — Create `FastAPI(title="PicklePoint API")` app. Add `CORSMiddleware` with `allow_origins=settings.origins_list`, `allow_credentials=True`, `allow_methods=["GET", "POST", "PUT", "DELETE", "OPTIONS"]`, `allow_headers=["Authorization", "Content-Type"]`. Include health router.

5. `backend/requirements.txt` — Pin: `fastapi>=0.115.0`, `uvicorn[standard]>=0.30.0`, `pydantic-settings>=2.0.0`, `supabase>=2.0.0`, `httpx>=0.27.0`, `pytest>=8.0.0`, `pytest-asyncio>=0.23.0`.

6. `backend/railway.toml` — Use nixpacks builder. Set `startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"`. Set `healthcheckPath = "/health"`, `healthcheckTimeout = 100`, `restartPolicyType = "on_failure"`.

7. `backend/.env.example` — Template with `SUPABASE_URL=`, `SUPABASE_SERVICE_ROLE_KEY=`, `ALLOWED_ORIGINS=http://localhost:3000`, `PORT=8000`.

IMPORTANT: Do NOT use `allow_origins=["*"]` with `allow_credentials=True` — FastAPI will raise RuntimeError. Always use explicit origins from settings.
  </action>
  <verify>
    <automated>cd backend && python -c "from config import get_settings; from main import app; print('imports ok')" 2>&1 || echo "Import check requires env vars — verify file structure exists"</automated>
  </verify>
  <done>All backend files exist with correct structure. main.py imports config and health router. Config uses pydantic-settings. railway.toml has correct start command with $PORT.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create pytest test suite for health and config</name>
  <files>backend/pytest.ini, backend/tests/__init__.py, backend/tests/test_health.py, backend/tests/test_config.py</files>
  <behavior>
    - test_health_returns_200: GET /health returns status code 200 and body {"status": "ok"}
    - test_settings_loads_from_env: Settings class loads SUPABASE_URL from environment variable
    - test_settings_origins_list_splits_comma: origins_list property splits "http://a.com,http://b.com" into list of 2
    - test_settings_missing_required_raises: Missing SUPABASE_URL raises validation error
  </behavior>
  <action>
1. `backend/pytest.ini` — Set `[pytest]` with `testpaths = tests` and `asyncio_mode = auto`.

2. `backend/tests/__init__.py` — empty file.

3. `backend/tests/test_health.py` — Use `httpx.AsyncClient` with `ASGITransport(app=app)` to test `GET /health`. Must set env vars `SUPABASE_URL=https://test.supabase.co` and `SUPABASE_SERVICE_ROLE_KEY=test-key` before importing app (use `monkeypatch` or `os.environ` in fixture). Verify status 200 and response JSON `{"status": "ok"}`.

4. `backend/tests/test_config.py` — Test that `Settings` loads `supabase_url` from env. Test that `origins_list` correctly splits comma-separated string. Test that missing required env vars raises `ValidationError`. Use `monkeypatch.setenv` to set test values, and clear `get_settings` cache between tests with `get_settings.cache_clear()`.

Install dependencies: `cd backend && pip install -r requirements.txt`
  </action>
  <verify>
    <automated>cd backend && pip install -r requirements.txt -q && pytest tests/ -x -q</automated>
  </verify>
  <done>All 4+ tests pass. pytest runs in under 5 seconds. Health endpoint returns 200. Config loads from env vars correctly.</done>
</task>

</tasks>

<verification>
cd backend && pytest tests/ -v
</verification>

<success_criteria>
- FastAPI app structure exists in /backend with main.py, config.py, routers/health.py
- `pytest tests/ -v` shows all tests passing
- railway.toml has correct deployment configuration
- .env.example documents all required environment variables
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffolding/01-01-SUMMARY.md`
</output>
