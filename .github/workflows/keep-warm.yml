name: Keep Railway Warm

on:
  schedule:
    - cron: "*/4 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - name: Ping health endpoint
        run: curl -f ${{ secrets.RAILWAY_BACKEND_URL }}/health || exit 1
